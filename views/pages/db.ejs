<!DOCTYPE html>
<html>
<head>
  <% include ../partials/header.ejs %>
</head>

<body>

<% include ../partials/nav.ejs %>

<div class="container">
<h2>Database Results</h2>

<ul>
    <% rows.forEach(function(r) { %>

        <li><%= r.name %> - <%= r.size %> - <%= r.height %> - <%= r.type %> - <%= r.salary  %></li>
    <% }); %>
</ul>

</div>
<div>
  <canvas width="1200" height="600" id="myCanvas" style="border:1px solid #000000;></canvas>
</div>
<script>

var canvas=document.getElementById("myCanvas");
var sky=canvas.getContext("2d");
var l=10;

<% rows.forEach(function(r) { %>

    <%if(r.type=='A'){ %>
      console.log("5");
      sky.fillStyle=rgb(200,0,0);
      sky.fillRect(l,30,<%=r.size%>,<%=r.height%>);
      l=l+<%=r.size%>;
    <%}%>

      <%if(r.type=='B'){ %>
        console.log("6");
        sky.fillStyle=rgb(0,200,0);
        sky.fillRect(l,30,<%=r.size%>,<%=r.height%>);
        l=l+<%=r.size%>;
      <%}%>
      <%if(r.type=='C'){ %>
        console.log("7");
        sky.fillStyle=rgb(0,0,200);
        sky.fillRect(l,30,<%=r.size%>,<%=r.height%>);
        l=l+<%=r.size%>;
      <%}%>
<% }); %>
</script>

</body>
</html>
